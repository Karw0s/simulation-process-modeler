import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class SimulationService {

  constructor() { }

  // todo: set parameters of simulation (time, sim_unit, sim_period(tempo), sim_end)
}
